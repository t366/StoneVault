# 需求实施计划：StoneVault（磐石 Vault）智能内网备份中枢

- [x] 1. 设置项目结构和核心接口
  - 创建后端目录结构：`backup_engine`、`scheduler`、`indexer`、`query_service`、`preview_service`、`transcode_worker`、`ai_indexer`、`wake_manager`、`auth`、`api` 模块
  - 创建 Sanic 应用入口与配置加载（挂载点、存储路径、冷区挂载路径）
  - 创建 Vue3 + Element Plus 前端工程骨架（Vite），配置 `/api` 反向代理到后端
  - 设置 pytest 测试框架与 `tests/` 目录
  - 引用：设计文档 Components 分层、需求 5 非功能（内网部署）

- [x] 2. 实现数据库初始化与数据模型
  - [x] 2.1 实现 SQLite 连接工具与 WAL 模式初始化
    - 编写连接管理、启用 `PRAGMA journal_mode=WAL`、外键约束
    - 为数据库操作创建错误处理工具
  - [x] 2.2 实现全部核心表 DDL 与迁移脚本
    - 编写 `tasks`、`snapshots`、`file_index`、`metadata`、`admin_user`、`ai_jobs` 建表语句
    - 实现版本化 schema 初始化函数
  - [x] 2.3 实现 FTS5 虚拟表 file_fts
    - 以外部内容表模式（`content='file_index'`）创建 `file_fts`，`rowid` 关联 `file_index.id`，使用 `trigram` 分词器
  - [x] 2.4 实现数据访问层（Repository）
    - 为每张表编写 CRUD 仓储类
  - [ ]* 2.5 为数据模型与仓储层编写单元测试
    - 验证建表、WAL 模式、FTS5 写入与同步

- [x] 3. 实现管理员认证（需求 10）
  - [x] 3.1 实现密码散列工具
    - 使用带盐单向散列（PBKDF2-SHA256）编写 `hash_password` 与 `verify_password`
  - [x] 3.2 实现登录/登出/会话校验
    - 编写 `/api/auth/login`、`/api/auth/logout` 接口与 HMAC 签名会话令牌签发
    - 编写认证中间件，拦截管理接口返回 401
  - [ ]* 3.3 为认证模块编写单元测试
    - 验证正确/错误凭据、过期会话、密码散列不可逆

- [x] 4. 实现备份引擎与双存储写入（需求 1、2、4）
  - [x] 4.1 实现文件过滤规则
    - 编写后缀白/黑名单与大小上下限过滤函数（含边界值）
  - [x] 4.2 实现全量备份流程
    - 扫描源目录 → 过滤 → 写入冷区完整副本 → 写入热区压缩副本 → 更新 `file_index`
    - 冷区写入先于热区缓存，MD5 校验通过后落索引
  - [x] 4.3 实现增量备份
    - 基于 `file_index` 该任务最近快照比较 mtime 与 MD5，仅传输变更/新增文件
  - [x] 4.4 实现快照生成与双写原子性
    - 每次备份生成 `snapshots` 记录（状态、文件数、总字节、错误信息）
    - 单文件任一步失败即标记失败，不产生半写入索引
  - [ ]* 4.5 为过滤规则与增量差异编写属性/单元测试
    - 属性：过滤函数对任意大小/后缀组合返回合法集合；增量集合恒为变更或新增文件的子集（设计文档属性 2）

- [x] 5. 实现任务管理与调度（需求 2、3）
  - [x] 5.1 实现任务 CRUD REST 接口
    - 编写 `/api/tasks` 增删改查与 `/api/tasks/{id}/run` 手动触发
  - [x] 5.2 实现 APScheduler 集成
    - 解析 Cron 表达式触发任务，实现错峰延时与传输限速
  - [x] 5.3 实现执行锁与幂等触发
    - 同一任务执行期间重复触发合并为单次，冲突任务标记 `skipped`（需求 3.4）
  - [x]* 5.4 为调度与幂等触发编写单元测试

- [ ] 6. 检查点 - 确保所有测试通过，如有疑问请询问用户

- [x] 7. 实现基础查询服务与前端框架（需求 5）
  - [x] 7.1 实现文件组合查询接口
    - 编写文件名模糊、后缀、时间范围、大小范围组合过滤与分页排序
    - 验证超过 10000 条索引记录时 2 秒内返回分页结果（需求 5.4）
  - [ ] 7.2 实现前端基础页面
    - 编写任务列表页、任务日志页、文件列表页（Element Plus）
  - [x]* 7.3 为查询过滤与分页编写单元测试

- [ ] 8. 实现在线预览（需求 6）
  - [ ] 8.1 实现图片缩略图与渲染
    - 使用 PIL 生成热区缩略图，编写图片预览接口（仅读热区）
  - [ ] 8.2 实现 PDF/TXT 网页预览
    - 编写文本/PDF 渲染接口（PDF.js 前端渲染，不依赖外网）
  - [ ] 8.3 实现前端预览组件
    - 编写图片/PDF/文本预览弹窗组件
  - [ ]* 8.4 为预览接口编写集成测试
    - 验证预览请求不触发冷区唤醒（需求 6.4）

- [ ] 9. 实现 FTS5 全文检索（需求 5）
  - [ ] 9.1 实现文档正文提取器
    - 编写 Word/PDF/TXT 正文提取并写入 `file_fts.body`（需求 7.1 文本来源）
  - [ ] 9.2 实现全文检索接口与高亮
    - 使用 `MATCH` 查询并返回 Snippet 与关键词高亮（需求 5.3）
  - [ ] 9.3 实现前端内容搜索框与高亮展示
  - [ ]* 9.4 为 FTS5 查询编写单元测试
    - 属性：任意查询词在 `file_fts` 上的匹配结果与 `file_index` 一一对应（设计文档属性 5）

- [ ] 10. 实现 HDD 按需唤醒与下载（需求 8）
  - [ ] 10.1 实现 wake_manager
    - 编写冷区唤醒/休眠控制、防抖窗口合并、超时重试（需求 8.1、8.3）
  - [ ] 10.2 实现源文件下载接口
    - 编写触发唤醒 + 流式传输的 `/api/files/{id}/download`，完成后释放唤醒锁（需求 8.4）
  - [ ] 10.3 实现冷区离线检测与前端提示
    - 编写冷区挂载检测与友好提示文案（需求 8.2）
  - [ ]* 10.4 为唤醒防抖编写属性测试
    - 属性：同一防抖窗口内并发请求仅触发一次唤醒（设计文档属性 3）

- [ ] 11. 实现音视频转码预览（需求 6、VPU 硬解优先）
  - [ ] 11.1 实现 transcode_worker
    - 优先使用 rkmpp 硬解码，失败自动回退 ffmpeg 软件解码，输出 HLS/MP4 分段
    - 限制转码进程并发数为 1（设计文档正确性属性 4）
  - [ ] 11.2 实现流式播放接口与前端播放器
  - [ ]* 11.3 为转码回退逻辑编写单元测试
    - 验证硬解失败时回退软解并保持输出一致

- [ ] 12. 检查点 - 确保所有测试通过，如有疑问请询问用户

- [ ] 13. 实现 AI 深度索引（需求 7、9）
  - [ ] 13.1 实现低优先级 RQ 任务队列
    - 编写队列、`ai_jobs` 状态流转与并发进程数上限 2 约束（需求 7.3）
  - [ ] 13.2 实现图片 OCR 接入
    - 以 CPU 版 PaddleOCR 先行实现，预留 RKNN/NPU 推理接口（需求 7.1）
  - [ ] 13.3 实现语音转写接入
    - 集成 Vosk 离线中文模型，提取文本写入 `file_fts.ai_text`（需求 7.2）
  - [ ] 13.4 实现深度索引调度与手动触发
    - 编写空闲时段（凌晨）自动调度与 `/api/index/deep` 手动触发（需求 7.4）
  - [ ] 13.5 实现内存优化与降级
    - 流式/分块处理大文件、任务结束后主动 gc 回收、内存压力时推迟 AI 任务（需求 9.2、9.3、9.4）
  - [ ]* 13.6 为 AI 队列并发上限编写属性测试
    - 属性：任意时刻运行中的 AI 进程数不超过 2（设计文档属性 4）
  - [ ]* 13.7 为 OCR/ASR 文本入库编写集成测试
    - 验证识别文本写入 `file_fts` 后可被全文检索命中

- [ ] 14. 实现系统状态与告警（需求 11）
  - [ ] 14.1 实现 `/api/system/status`
    - 返回 HDD/SSD 状态、内存占用、AI 队列深度
  - [ ] 14.2 实现告警与日志展示
    - 编写任务失败、冷区离线、磁盘空间不足的告警状态与前端展示（需求 11.3）
  - [ ]* 14.3 为告警状态生成逻辑编写单元测试

- [ ] 15. 实现存储空间清理策略（第三阶段）
  - [ ] 15.1 实现过期缩略图与转码临时分段清理
  - [ ] 15.2 实现热区缓存副本 LRU 淘汰
    - 淘汰策略确保不删除冷区源文件副本

- [ ] 16. 检查点 - 最终确保所有测试通过，如有疑问请询问用户
